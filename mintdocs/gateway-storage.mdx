---
title: "Gateway storage and config format"
description: "Default storage locations, file layout, and JSON formats for gateway instances."
icon: "database"
---

Gateway data is stored under the platform app data directory for `phbgateway`.

## Default storage locations

- Windows: `%LOCALAPPDATA%\\phbgateway`
- macOS: `~/Library/Application Support/phbgateway`
- Linux: `~/.local/share/phbgateway`

## Directory structure

```text
<app_data>/phbgateway/
  registry.json
  instances/
    <name>/
      config.json
      gateway.pid
      logs/
```

## `registry.json` format

`registry.json` tracks all instances and the default instance.

```json
{
  "default_instance": "home",
  "instances": {
    "home": {
      "name": "home",
      "path": "C:/Users/<user>/AppData/Local/phbgateway/instances/home",
      "host": "0.0.0.0",
      "port": 8765
    }
  }
}
```

### Fields

- `default_instance`: instance name used when `--instance` is omitted
- `instances`: map keyed by instance name
  - `name`: instance name
  - `path`: absolute path to instance folder
  - `host`: bind host
  - `port`: bind port

## `config.json` format

Each instance has its own `config.json`.

```json
{
  "desktop_public_key": "<base64-ed25519-public-key>",
  "log_dir": ""
}
```

### Fields

- `desktop_public_key`: mandatory trust root configured at `instance create`
- `log_dir`: optional persistent log directory override
  - empty string means default: `<instance>/logs`

## `gateway.pid`

`gateway.pid` stores the active process ID for stop/status operations.

- created on background `start`
- removed on successful `stop` or when stale PID is detected

## Logs

Logs are written to:

- `config.log_dir` if configured
- otherwise `<instance>/logs`

You can also pass `phbgateway start --log-dir <path>` for a runtime-only override.
