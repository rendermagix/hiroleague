# phb-commons

Shared utility modules for Private Home Box services.

This package is intended to host reusable functionality used by multiple
workspace members (for example `phbcli` and `gateway`), including crypto,
timestamp, and other cross-cutting helpers.
